<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".AlfaNewsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlfaNews"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <property android:name="android.supported_16k_page_size" android:value="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AlfaNews.Splash"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: https://alfanews.app and https://www.alfanews.app (HTTPS only) -->
            <!-- autoVerify="true" tells Android to verify this app is the handler via assetlinks.json -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="alfanews.app" android:pathPrefix="/news" />
                <data android:scheme="https" android:host="www.alfanews.app" android:pathPrefix="/news" />
            </intent-filter>

            <!-- Custom Scheme Deep Link: alfanews://news/{postId} -->
            <!-- This is a fallback for when App Links verification might fail -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="alfanews" android:host="news" />
            </intent-filter>

            <!-- Reporter Deep Link: https://alfanews.app/verify/{reporterId} -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="alfanews.app" android:pathPrefix="/verify" />
                <data android:scheme="https" android:host="www.alfanews.app" android:pathPrefix="/verify" />
                <data android:scheme="https" android:host="alfanews.app" android:pathPrefix="/reporter" />
                <data android:scheme="https" android:host="www.alfanews.app" android:pathPrefix="/reporter" />
            </intent-filter>

            <!-- Custom Scheme Reporter Deep Link: alfanews://verify/{reporterId} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="alfanews" android:host="verify" />
                <data android:scheme="alfanews" android:host="reporter" />
            </intent-filter>
        </activity>

        <!-- Firebase Dynamic Links Configuration -->
        <!-- Enables automatic handling of Firebase Dynamic Links for app installation fallback -->
        <meta-data
            android:name="com.google.firebase.dynamiclinks.DEFAULT_URL"
            android:value="https://alfanews.page.link" />

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5787901991150360~2930000410" />

        <meta-data
            android:name="com.google.android.gms.ads.flag.PRELOAD_NATIVE_ADS"
            android:value="true" />

        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver android:name=".services.NotificationActionReceiver" android:exported="false">
            <intent-filter>
                <action android:name="com.alfanews.telugu.ACTION_SHARE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
