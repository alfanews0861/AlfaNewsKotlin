<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.AlfaNews" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme. -->
    <style name="Theme.AlfaNews.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@drawable/splash_screen_drawable</item>
        <item name="postSplashScreenTheme">@style/Theme.AlfaNews</item>
    </style>
</resources>
